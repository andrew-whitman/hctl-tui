#!/usr/bin/env bash
# Native git pre-commit hook: auto-bump package version when needed.
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
BUMP="$ROOT/scripts/pre_commit_bump_version.py"

if [[ ! -f "$BUMP" ]]; then
  echo "error: missing $BUMP" >&2
  exit 1
fi

exec python3 "$BUMP"
