#!/usr/bin/env zsh
# Local checkout launcher — prefers packaged share next to this repo.
set -euo pipefail
ROOT="$(cd -- "${0:A:h}/.." && pwd)"
SHARE="$ROOT/src/hctl_tui/share"
export HTS_ROOT="$SHARE"
export HTS_LIB="$SHARE/lib"
exec zsh "$SHARE/bin/hts" "$@"
