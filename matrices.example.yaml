# Example matrix template.
# Live files: ~/.config/hctl-tui/matrices/<hctl-profile>/<module>.yaml
#
# type: github (default) — get-trigger → resolve inputYaml → pipeline execute
# type: custom — POST Harness custom webhook
#
# Auth belongs in ~/.config/hctl via `hts init` / `hctl init`.

module: ci
entries: []
# Example entry shape (uncomment and edit):
# entries:
#   - alias: my-alias
#     type: github
#     trigger: MY_GITHUB_TRIGGER_ID
#     tech: java
#     set: shared
#     pipeline:
#       org: YOUR_ORG
#       project: YOUR_PROJECT
#       identifier: YOUR_PIPELINE_ID
